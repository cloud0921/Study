import numpy as np

# 공부한 시간과 점수를 각각 x, y라는 이름의 넘파이 배열로 만듭니다.

x = np.array([2, 4, 6 ,8])
y = np.array([81, 93, 91, 97])

print(x)
print(y)

# 파이썬에서 리스트는 대괄호([])로 감싼 요소들을 쉼표(,)로 구분해 대입하여 만듦
# np.array()함수를 사용하면 파이썬 리스트를 넘파이 배열로 바꾸어 여러가지 계산을 수행할 수 있음